import {StyleSheet, Dimensions} from 'react-native';
import colors from '../colors';
 
const styMovie = StyleSheet.create({
    FLatItem: {
      marginLeft: 3,
    },
  });

  export default styMovie;