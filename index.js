/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Click from './src/navigation/Click';
import './ignoreWarning';
AppRegistry.registerComponent(appName, () => Click);
